type: object
description: >
  Wrapper for all supported resources in a ChangeEvent.
  Only the resource corresponding to change_resource_type will be set.
properties:
  ad:
    $ref: '#/components/schemas/Ad'
    description: >
      Output only. Set if change_resource_type == AD.
    readOnly: true

  ad_group:
    $ref: '#/components/schemas/AdGroup'
    description: >
      Output only. Set if change_resource_type == AD_GROUP.
    readOnly: true

  ad_group_criterion:
    $ref: '#/components/schemas/AdGroupCriterion'
    description: >
      Output only. Set if change_resource_type == AD_GROUP_CRITERION.
    readOnly: true

  campaign:
    $ref: '#/components/schemas/Campaign'
    description: >
      Output only. Set if change_resource_type == CAMPAIGN.
    readOnly: true

  campaign_budget:
    $ref: '#/components/schemas/CampaignBudget'
    description: >
      Output only. Set if change_resource_type == CAMPAIGN_BUDGET.
    readOnly: true

  ad_group_bid_modifier:
    $ref: '#/components/schemas/AdGroupBidModifier'
    description: >
      Output only. Set if change_resource_type == AD_GROUP_BID_MODIFIER.
    readOnly: true

  campaign_criterion:
    $ref: '#/components/schemas/CampaignCriterion'
    description: >
      Output only. Set if change_resource_type == CAMPAIGN_CRITERION.
    readOnly: true

  ad_group_ad:
    $ref: '#/components/schemas/AdGroupAd'
    description: >
      Output only. Set if change_resource_type == AD_GROUP_AD.
    readOnly: true

  asset:
    $ref: '#/components/schemas/Asset'
    description: >
      Output only. Set if change_resource_type == ASSET.
    readOnly: true

  customer_asset:
    $ref: '#/components/schemas/CustomerAsset'
    description: >
      Output only. Set if change_resource_type == CUSTOMER_ASSET.
    readOnly: true

  campaign_asset:
    $ref: '#/components/schemas/CampaignAsset'
    description: >
      Output only. Set if change_resource_type == CAMPAIGN_ASSET.
    readOnly: true

  ad_group_asset:
    $ref: '#/components/schemas/AdGroupAsset'
    description: >
      Output only. Set if change_resource_type == AD_GROUP_ASSET.
    readOnly: true

  asset_set:
    $ref: '#/components/schemas/AssetSet'
    description: >
      Output only. Set if change_resource_type == ASSET_SET.
    readOnly: true

  asset_set_asset:
    $ref: '#/components/schemas/AssetSetAsset'
    description: >
      Output only. Set if change_resource_type == ASSET_SET_ASSET.
    readOnly: true

  campaign_asset_set:
    $ref: '#/components/schemas/CampaignAssetSet'
    description: >
      Output only. Set if change_resource_type == CAMPAIGN_ASSET_SET.
    readOnly: true
